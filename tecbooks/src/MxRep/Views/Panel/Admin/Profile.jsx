import React from 'react'

function Profile() {
  // like all profile views, should simply display the user's profile
  // as a form, that it editable and can be saved. should include
  // then a hook as interface for an api to gett all the admin and 
  // institution's data, not just the limited data of user in the auth context


  return (
    <div>Profile</div>
  )
}

export default Profile