import React from 'react'

function Student() {
  // should include hook for getting the student's data
  // should present a form with the student's data
  // should include a button to edit the student's data
  // should include a button to delete the student

  return (
    <div>Student</div>
  )
}

export default Student