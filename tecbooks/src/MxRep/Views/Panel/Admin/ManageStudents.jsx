import React from 'react'

function ManageStudents() {
  // should include hook for getting all students for the institution
  // should present a table with a row for each student
  // clicking on a student will navigate to the student view
  // should probably only show like 15 students at a time and include pagination

  return (
    <div>
      ManageStudents
    </div>
  )
}

export default ManageStudents